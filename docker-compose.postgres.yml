services:
  postgres:
    image: postgres:16-alpine
    container_name: silver-postgres
    environment:
      POSTGRES_DB: silver
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - silver_pg_data:/var/lib/postgresql/data

volumes:
  silver_pg_data:
